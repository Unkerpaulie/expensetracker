# Django Expense Tracker Environment Configuration
# Copy this file to .env and update with your actual values

# =============================================================================
# ENVIRONMENT SELECTION
# =============================================================================
# Options: 'local', 'railway', 'pythonanywhere'
# This determines which configuration set to use
ENVIRONMENT=local

# =============================================================================
# DJANGO CORE SETTINGS
# =============================================================================
# Secret key for Django (IMPORTANT: Use a unique, random string in production!)
SECRET_KEY=django-insecure-4wp46j4_r5(4v_3&jp2m-68j$p9ny)qylj%_(&n^k^q9xz&jn%

# Debug mode (set to False in production)
DEBUG=True

# =============================================================================
# LOCAL ENVIRONMENT SETTINGS
# =============================================================================
# For local development, no additional settings needed
# Uses SQLite database by default

# =============================================================================
# RAILWAY ENVIRONMENT SETTINGS
# =============================================================================
# When ENVIRONMENT=railway, configure these:

# Allowed hosts (comma-separated, no spaces)
# ALLOWED_HOSTS=expensetracker-production-b92e.up.railway.app,*.railway.app

# Database URL (Railway provides this automatically)
# DATABASE_URL=postgresql://user:password@host:port/database

# Railway public domain (for CSRF)
# RAILWAY_PUBLIC_DOMAIN=expensetracker-production-b92e.up.railway.app

# =============================================================================
# PYTHONANYWHERE ENVIRONMENT SETTINGS
# =============================================================================
# When ENVIRONMENT=pythonanywhere, configure these:

# Allowed hosts (comma-separated, no spaces)
# ALLOWED_HOSTS=unkerpaulie.pythonanywhere.com

# PythonAnywhere domain (for CSRF)
# PYTHONANYWHERE_DOMAIN=unkerpaulie.pythonanywhere.com

# Database URL (optional - if not set, uses SQLite)
# For MySQL on PythonAnywhere:
# DATABASE_URL=mysql://username:password@username.mysql.pythonanywhere-services.com/username$dbname

# =============================================================================
# NOTES
# =============================================================================
# 1. Never commit the actual .env file to version control
# 2. Generate a new SECRET_KEY for production using:
#    python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
# 3. Always set DEBUG=False in production
# 4. For Railway: Most settings are auto-configured via Railway's environment
# 5. For PythonAnywhere: Configure via their web interface or .env file

