# Railway Environment Configuration
# Set these as environment variables in Railway dashboard

ENVIRONMENT=railway
DEBUG=False

# Generate a new secret key for production:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-production-secret-key-here

# Railway auto-provides DATABASE_URL for PostgreSQL
# DATABASE_URL is automatically set by Railway when you add a PostgreSQL service

# Set your Railway domain (Railway provides RAILWAY_STATIC_URL, but set this for CSRF)
RAILWAY_PUBLIC_DOMAIN=expensetracker-production-b92e.up.railway.app

# Allowed hosts - use your Railway domain
ALLOWED_HOSTS=expensetracker-production-b92e.up.railway.app,*.railway.app

# Railway will automatically set PORT - no need to configure

